import { DashboardTableFilterPipe } from './dashboard-table-filter.pipe';

describe('DashboardTableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardTableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
